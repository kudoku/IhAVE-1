<h1>Locations#edit</h1>
<p>Find me in app/views/locations/edit.html.erb</p>

<%= form_for(@location, method: :patch) do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :description %>
	<%= f.text_field :description %>

	<%= f.submit %>
<% end %>