
    <div class = "item-img">
      <%= image_tag @item.avatar.url(:medium) %>
    </div>
    <div class = item-info>

      <h3>Description:<%=  @item.description %></h4>
      <h3>Price:<%=@item.price %></h3>
      <%= @item.tag_list %>
      <h3>Quantity:&nbsp  <%=@item.quantity %></h3>
      <p class="text-center">
        <%= link_to "Delete", location_item_path(@location, @item), method: :delete, class: "btn btn-danger btn-s" %>
        <%= link_to "Edit", edit_location_item_path(@location, @item),class: "btn btn-primary btn-s" %>
      </p>
    </div>
