$('#modal-div').html('<%= j render "modal" %>');
$('#newItem').modal('show');
console.log('this worked');

$(function() {
	$('#item_is_borrowed').change(function() {
		// alert('hi');
		if($('#item_is_borrowed').prop('checked')) {
			// alert('checked');
			$('.record-form').show();
		}
		else {
			// alert('unchecked');
			$('.record-form').hide();
		}
	});

	$('.date').datepicker({
		dateFormat: 'yy-mm-dd'
	});

	$("#item_tag_list").tagit();
	
});