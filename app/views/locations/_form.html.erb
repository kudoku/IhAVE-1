<%= form_for(@location) do |f| %>
    <%= f.file_field :avatar %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.submit %>
<% end %>