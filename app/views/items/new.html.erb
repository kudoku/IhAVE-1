<%= bootstrap_form_for [@location, @item] do |f| %>


  <%= f.text_field :name %>

  <%= f.text_area :description %>

  <%= f.check_box :is_out %>
  
  <% if @item.is_out %> 
     <%= f.check_box :is_out %>

   <%= f.datetime :due_date %>
    <%end%>

		<%= f.text_field :tag_list %>


  <%= f.submit %>

<% end %>

	
